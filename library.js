let book = [];

// funtion to add a book
const newBook = {
    title: "College Mathematics"
    author: "Ho Soo Thong"
    year: "2002"
    genre: "Fiction"
}
function addABook () {
    if newBook (title && author && year && genre) {
        book.push(newBook)
    }
}

// function to remove a book
function removeABook () {
    book.shift(newBook)
}

